#include <stdio.h>

/**
 *main - main block, use puts to output string to stdout
 *with newline by default
 *
 *return: 0
 */

int main(void)
{
	puts("\"Programming is like building a multilingual puzzle");
	return (0);
}
