#include <stdio.h>

/**
 *Use puts to output string to stdout
 *with newline by default
 *
 * Description: print to standard io
 * return 0
 */

int main(void)
{
	puts("\"Programming is like building a multilingual puzzle");
	return (0);
}
