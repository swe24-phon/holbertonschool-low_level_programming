#include <stdio.h>
#include "main.h"

/**
 * _strcpy - copy string
 *
 * @dest: destination to copy to
 * @src: orginal source
 *
 * Return: 0 on sucess
 *
 */

int _atoi(char *s)
{
	int i = 0;
	int neg = 1;

	while (s[i] != '\0')
	{
		if (s[i] == 45)
			neg *= -1;
		if (s[i] >= 48 && s[i] <= 57)
	}
	return (0);
}
